# A minimal software release to test the gcc component.

[buildout]
extends = 
  ../../stack/slapos.cfg
  ../../component/gcc/buildout.cfg

parts =
  eggs
  gcc-fortran

[eggs]
recipe = zc.recipe.egg
eggs =
  ${lxml-python:egg}
  slapos.cookbook

