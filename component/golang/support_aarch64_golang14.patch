--- src/cmd/dist/unix.c.orig	2023-01-05 15:13:09.268098224 +0000
+++ src/cmd/dist/unix.c	2023-01-05 15:07:28.915485172 +0000
@@ -706,7 +706,7 @@
 			gohostarch = "amd64";
 		else if(hassuffix(u.machine, "86"))
 			gohostarch = "386";
-		else if(contains(u.machine, "arm"))
+		else if(contains(u.machine, "arm") || contains(u.machine, "aarch64"))
 			gohostarch = "arm";
 		else
 			fatal("unknown architecture: %s", u.machine);

