#!/bin/bash
#move all files in srv/www/matomo to srv/www
#so we can directly access by url root

set -e
set -x

if [ ! -d {{ parameter_dict['document-root'] }} ]; then
  exit 1;
fi

cd {{ parameter_dict['document-root'] }}
mv ./matomo/* .
rmdir ./matomo
rm ./How to install Matomo.html

# while :
# do
#   if [-f ./config/config.ini.php]; then
#     echo "[General]" >> ./config/config.ini.php 
#     echo "enable_trusted_host_check=0" >> ./config/config.ini.php 
#     break
#   fi
#   sleep 1
# done


