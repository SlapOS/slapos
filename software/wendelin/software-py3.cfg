[buildout]
versions = versions
extends =
  ../../component/msgpack/buildout.cfg
  ../../software/erp5/software-py3.cfg
  base.cfg

parts +=
  wendelin
  ipython

[wendelin]
# Use specific branch until the changes
# are merged.
branch = zope4py3
