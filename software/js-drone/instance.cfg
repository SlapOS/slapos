[buildout]
parts =
  cli-js
  demo-js

eggs-directory = ${buildout:eggs-directory}
develop-eggs-directory = ${buildout:develop-eggs-directory}
offline = true

[directory]
recipe = slapos.cookbook:mkdirectory
home = $${buildout:directory}
etc = $${:home}/etc
var = $${:home}/var
log = $${:var}/log

[slap-configuration]
recipe = slapos.cookbook:slapconfiguration
computer = $${slap_connection:computer_id}
partition = $${slap_connection:partition_id}
url = $${slap_connection:server_url}
key = $${slap_connection:key_file}
cert = $${slap_connection:cert_file}

[dynamic-template-base]
recipe = slapos.recipe.template:jinja2
rendered = $${directory:etc}/$${:filename}
context =
  key ipv6 slap-configuration:ipv6-random
  key log_dir directory:log
  raw mavsdk ${mavsdk-qjs-wrapper:location}/lib/libmavsdkqjs.so
  raw open62541 ${open62541-qjs-wrapper:location}/lib/libopen62541qjs.so

[cli-js]
< = dynamic-template-base
template = ${cli:location}/${cli:filename}
filename = ${cli:filename}

[demo-js]
< = dynamic-template-base
template = ${demo:location}/${demo:filename}
filename = ${demo:filename}
