[buildout]
parts =
  switch_softwaretype
eggs-directory = ${buildout:eggs-directory}
develop-eggs-directory = ${buildout:develop-eggs-directory}

[switch_softwaretype]
recipe = slapos.cookbook:softwaretype
default = $${instance-proview-r:rendered}

[template-base]
recipe = slapos.recipe.template:jinja2
rendered = $${buildout:directory}/$${:filename}
extensions = jinja2.ext.do
mode = 0644
extra-context =
context =
  key develop_eggs_directory buildout:develop-eggs-directory
  key eggs_directory buildout:eggs-directory
  key ipv4 slap-configuration:ipv4
  key ipv6 slap-configuration:ipv6
  key global_ipv4_prefix network-information:global-ipv4-network
  key slapparameter_dict slap-configuration:configuration
  key computer_id slap-configuration:computer
  raw openssl_executable_location ${openssl:location}/bin/openssl
  raw template_logrotate ${template-logrotate-base:rendered}
  raw template_monitor ${monitor2-template:rendered}
  $${:extra-context}

[instance-proview-r]
<= template-base
template = ${template-kvm-cluster:rendered}
rendered = $${buildout:directory}/template-proview-r.cfg
extensions = jinja2.ext.do
extra-context =
  key novnc_location noVNC:location
  raw websockify_executable_location ${buildout:directory}/bin/websockify
  raw xorg_font_util ${xorg-font-util:location}
  raw xvfb_bin ${xserver:location}/bin/Xvfb
  raw proview_directory ${pwd:location}
  raw ld_library_path ${pwd:ld-library-path}
mode = 0644

[slap-configuration]
recipe = slapos.cookbook:slapconfiguration.serialised
computer = $${slap-connection:computer-id}
partition = $${slap-connection:partition-id}
url = $${slap-connection:server-url}
key = $${slap-connection:key-file}
cert = $${slap-connection:cert-file}

