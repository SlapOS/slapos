{
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Input Parameters",
  "properties": {
    "prometheus": {
      "title": "Prometheus Metrics",
      "description": "Enable Prometheus metrics endpoint",
      "type": "boolean",
      "default": true
    },
    "append-only": {
      "title": "Append Only",
      "description": "Enable append only mode, preventing deletion of backup data",
      "type": "boolean",
      "default": true
    }
  }
}
