{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Values returned by Restic REST Server",
  "type": "object",
  "properties": {
    "rest-server-user": {
      "description": "Username for REST server authentication",
      "type": "string"
    },
    "rest-server-password": {
      "description": "Password for REST server authentication",
      "type": "string"
    },
    "backend-url": {
      "description": "Direct URL of the REST server. The CA certificate must be retrieved from caucase-url",
      "type": "string"
    },
    "caucase-url": {
      "description": "URL of Caucase managing the server certificate",
      "type": "string"
    },
    "frontend-url": {
      "description": "URL of the REST server, behind a CDN frontend, with a certificate that should be trusted directly, at the cost of less optimal performance that directly using backend-url",
      "type": "string"
    }
  }
}
