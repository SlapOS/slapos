{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "Input Parameters",
  "type": "object",
  "properties": {
    "promise_cpu_temperature_threshold": {
      "default": "90",
      "title": "CPU temperature promise threshold",
      "description": "Temperature threshold above which CPU temperature promise will fail",
      "type": "number"
    },
    "promise_cpu_avg_temperature_threshold": {
      "default": "80",
      "title": "Average CPU temperature promise threshold",
      "description": "If average temperature over specified duration reaches this threshold, promise will fail",
      "type": "number"
    },
    "promise_cpu_avg_temperature_threshold_duration": {
      "default": "600",
      "title": "Average CPU temperature promise threshold duration",
      "description": "Duration during which average temperature should not exceed specified threshold",
      "type": "number"
    },
    "promise_ram_available_threshold": {
      "default": "500e6",
      "title": "Minimum available RAM promise threshold",
      "description": "Minimum available RAM threshold below which RAM available promise will fail",
      "type": "number"
    },
    "promise_ram_avg_available_threshold": {
      "default": "1e9",
      "title": "Minimum average available RAM promise threshold",
      "description": "If average available ram over specified duration reaches this threshold, promise will fail",
      "type": "number"
    },
    "promise_ram_avg_available_threshold_duration": {
      "default": "600",
      "title": "Minimum average available RAM promise threshold duration",
      "description": "Duration during which average available RAM shall not be below specified threshold",
      "type": "number"
    },
    "promise_network_errors_threshold": {
      "default": "100",
      "title": "Maximum errors on Network per MB",
      "description": "Maximum network errors threshold(per MB) above which the promise will fail",
      "type": "number"
    },
    "promise_network_lost_packets_threshold": {
      "default": "100",
      "title": "Maximum lost packets on Network per MB",
      "description": "Maximum network lost packets threshold(per MB) above which the promise will fail",
      "type": "number"
    },
    "promise_network_transit_recv_threshold": {
      "default": "100",
      "title": "Minimum received packets on Network in bytes",
      "description": "Minimum received packets in bytes below which the promise will fail",
      "type": "number"
    },
    "promise_network_transit_sent_threshold": {
      "default": "100",
      "title": "Minimum sent packets on Network in bytes",
      "description": "Minimum sent packets in bytes below which the promise will fail",
      "type": "number"
    },
    "promise_network_transit_duration": {
      "default": "600",
      "title": "Average network transit promise threshold duration",
      "description": "Duration during which recv and sent network shall not be below specified thresholds",
      "type": "number"
    },
    "promise_cpu_load_threshold": {
      "default": "1.5",
      "title": "Maximum CPU load ratio threshold",
      "description": "Maximum CPU load ratio threshold above which the promise will fail",
      "type": "number"
    },
    "promise_partition_space_threshold": {
      "default": "0.08",
      "title": "Minimum partition space ratio threshold",
      "description": "Minimum partition space ratio threshold below which the promise will fail",
      "type": "number"
    },
    "promise_free_disk_space_threshold": {
      "default": "0.08",
      "title": "Minimum partition space ratio threshold",
      "description": "Minimum partition space ratio threshold below which the promise will fail",
      "type": "number"
    },
    "promise_free_disk_space_nb_days_predicted": {
      "default": "10",
      "title": "Number of days for prediction",
      "description": "Number of days that will be take into account for prediction calculation",
      "type": "number"
    },
    "promise_free_disk_space_display_partition": {
      "default": "1",
      "title": "Boolean to display partition",
      "description": "Enable partition display by setting Boolean to 1 ",
      "type": "number"
    },
    "promise_free_disk_space_display_prediction": {
      "default": "1",
      "title": "Boolean to display predition",
      "description": "Enable prediction display by setting Boolean to 1 ",
      "type": "number"
    }
  }
}
