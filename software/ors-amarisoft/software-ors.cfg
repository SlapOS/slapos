# software for Open Radio Station.
#
# It builds on generic software but exposes simplified configuration interface
# and leverages some ORS-specific features.
[buildout]
extends =
  software-base.cfg

# XXX move ors-specific code here

# XXX predefine ru/cells from ORS-specific schema, not from shared instances.

# XXX set [default-params] ?
