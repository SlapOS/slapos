# THIS IS NOT A BUILDOUT FILE, despite purposedly using a compatible syntax.
# The only allowed lines here are (regexes):
# - "^#" comments, copied verbatim
# - "^[" section beginings, copied verbatim
# - lines containing an "=" sign which must fit in the following categorie.
#   - "^\s*filename\s*=\s*path\s*$" where "path" is relative to this file
#     Copied verbatim.
#   - "^\s*hashtype\s*=.*" where "hashtype" is one of the values supported
#     by the re-generation script.
#     Re-generated.
# - other lines are copied verbatim
# Substitution (${...:...}), extension ([buildout] extends = ...) and
# section inheritance (< = ...) are NOT supported (but you should really
# not need these here).

[template-cfg]
filename = instance.cfg.in
md5sum = 83326ca5a8c2488c896f750327ce1b13

[template_development_yaml]
_update_hash_filename_ = templates/development_yaml.in
md5sum = 78721ea5454d08fd89b1ae095056744a

[template_production_yaml]
_update_hash_filename_ = templates/production_yaml.in
md5sum = d0085646274cb8b4a94323625e1f99a7

[template_config_yaml]
_update_hash_filename_ = templates/config_yaml.in
md5sum = 73eedd34d9395335bdc11f805f5f053f