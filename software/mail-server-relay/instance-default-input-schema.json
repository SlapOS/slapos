{
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Input Parameters",
  "properties": {
    "request_dns": {
      "title": "Request DNS",
      "description": "Request local DNS to a core network running on the same node",
      "type": "boolean",
      "default": false
    },
    "relay_host": {
      "title": "Relay Host",
      "description": "The host to relay the emails to",
      "type": "string"
    },
    "relay_port": {
      "title": "Relay Port",
      "description": "The port used by the relay",
      "type": "integer",
      "default": 25
    },
    "relay_user": {
      "title": "Relay User",
      "description": "The user to authenticate with the relay host",
      "type": "string"
    },
    "relay_password": {
      "title": "Relay Password",
      "description": "The password to authenticate with the relay host",
      "type": "string"
    },
    "mail_domains": {
      "title": "Mail Domains",
      "description": "List of domains that this relay server will consider internal",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The domain name"
          },
          "mail_server_host": {
            "type": "string",
            "description": "The mail server host (IPv6) for this domain",
            "title": "Mail Server Host"
          },
          "mail_server_prefix_size": {
            "type": "string",
            "description": "The IPv6 prefix size to use for the host address. This determines the range that is allowed to send mail through the relay.",
            "title": "Mail Server Prefix Size",
            "default": 128
          },
          "mail_server_port": {
            "type": "integer",
            "description": "The mail server port for this domain",
            "title": "Mail Server Port",
            "default": 10025
          }
        }
      },
      "default": []
    }
  }
}
