{#- SASL credentials for custom outbound relay authentication. -#}
{#- Maps [host]:port to user:password for each domain that has SASL credentials. -#}
[{{ out['host'] }}]:{{ out['port'] }} {{ out['user'] }}:{{ out['password'] }}
