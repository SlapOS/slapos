[SURYKATKA]
INTERVAL = 120
TIMEOUT = {{ timeout }}
SQLITE = {{ db }}
{%- if len(nameserver_list) > 0 %}
NAMESERVER =
{%-   for nameserver_entry in sorted(nameserver_list) %}
  {{ nameserver_entry }}
{%-   endfor %}
{% endif %}
URL =
{%- for slave in slave_instance_list | sort(attribute='_title') %}
{%-   if 'url' in slave %}
  {{ slave['url'] }}
{%-   endif -%}
{% endfor %}

