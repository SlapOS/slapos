#!/bin/bash

PID=$$
PIDFILE=~/my.pid

if [ ! -f $PIDFILE ]; then
  echo $PID >> $PIDFILE
else
  ps --pid `cat $PIDFILE` &>/dev/null
  if [ $? -eq 0 ]; then
    echo "Skipped"
  else
    echo $PID >> $PIDFILE
    sleep {{ wait_time }}
    {{ lazy_command }}
  fi
fi

