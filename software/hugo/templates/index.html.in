<<<<<<< HEAD
<h1 style="text-align: center;"><img src="https://html-online.com/editor/tiny4_9_11/plugins/emoticons/img/smiley-cool.gif" alt="cool" />Welcome to <a href="https://gohugo.io/" target="_blank">Hugo</a>'s workplace</h1>
<p>Here are some instructions you might need:&nbsp;</p>
<ol>
<li>To get started, please <code>source</code> the <code>go.work</code>'s environment path by typing the following command in your terminal:<br /><code>$ source {{ env }}<br /></code>You can also find the path (<code>env</code>) from the published information of the request script.</li>
<li>Once you see <code>(go.work) bash-4.3$</code> displayed on your terminal, you can follow the official documentation from Hugo to a <a href="https://gohugo.io/getting-started/quick-start/">quick start</a>:
<ul>
<li style="list-style-type: none;">
<ul>
<li>It is necessary to define the bind-address to launch the development website, eg. <code>hugo server -D --bind="ipv6_addr"</code></li>
<li><code>hugo</code> to create a public directory containing all the static files (index.html, etc..)</li>
<li>locate the directory of the site. eg. <code>/srv/slapgrid/slappart19/srv/project/quickstart</code>, while <code>index.html</code> is located <code>/srv/slapgrid/slappart19/srv/project/quickstart/public/index.html</code></li>
</ul>
</li>
</ul>
</li>
<li>Pass the <code>site-dir</code> by <code>--parameter site-dir='pubic_directory'</code> with slapos request command, eg.&nbsp;<br /><code>slapos request $software_name'_1' $software_release_uri --parameters site-dir='/srv/slapgrid/slappart19/srv/project/quickstart/public'</code></li>
<li><code>update-hash</code> and <code>slapos node software --all &amp;&amp; slapos node instance --all</code> if the autorun is not on.</li>
<li>Retrieve the CDN URL by re-launching the slapos request command (It should be the same URL as the one you are using now)</li>
</ol>
<p style="text-align: center;"><strong>Now you can access your published website powered by Hugo</strong></p>
=======
<p>hello temp</p>
>>>>>>> 70a9c56f676c2106ee73255a673d69bc7e768798
