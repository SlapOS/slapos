#! {{ param_hugo['path_shell'] }}
. {{ go_environment }}
cd {{ data }}
if [ {{ site }} = "default" ]; then
  hugo server --bind={{ ip }} --port={{ port }}
else
  cd {{ site }}
  if [ -d "public" ]; then rm -Rf public; fi
  hugo && hugo server --bind={{ ip }} --port={{ port }} --baseURL={{ base_url }} --appendPort=false  
fi
